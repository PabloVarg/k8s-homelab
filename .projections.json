{
  "*": {
    "make": "kubectl apply -k {dirname}",
    "start": "kubectl apply -k ."
  },
  "*job.yml": {
    "make": "kubectl apply -k {dirname}",
    "console": "kubectl delete jobs.batch {dirname}"
  }
}

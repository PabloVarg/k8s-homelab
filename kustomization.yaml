resources:
  - ingress
  - monitoring
  - certificates
  - apps

secretGenerator:
  - name: env-secrets
    envs:
      - ./environments/.env
  - name: cert-secrets
    namespace: cert-manager
    envs:
      - ./environments/.env.cert

generatorOptions:
  disableNameSuffixHash: true
